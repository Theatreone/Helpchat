import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    outDir: 'dist/public',
    lib: {
      entry: 'widget/helpchat.ts',
      name: 'HelpChat',
      fileName: () => 'helpchat.js',
      formats: ['iife']
    },
    minify: 'esbuild',
    target: 'es2020'
  }
});
